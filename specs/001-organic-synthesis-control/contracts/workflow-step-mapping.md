# Contract: `businessPhase` → 状态栏「当前动作」

**Normative**：与 [plan.md](../plan.md)、[spec.md](../spec.md) FR-004 / FR-011 一致。

## 映射表（恒等 + 覆盖）

| `RecipeStep.businessPhase` | 状态栏显示（默认） |
|----------------------------|-------------------|
| 加粉 | 加粉 |
| 反应 | 反应 |
| 萃取 | 萃取 |
| 蒸馏 | 蒸馏 |
| 过滤 | 过滤 |
| 取样 | 取样 |

## 规则

1. 若 `displayOverride` 非空，状态栏 **MUST** 显示该字符串（中文、可读）。
2. 未知枚举值：实现 **MUST NOT** 静默；应记录错误并显示安全占位文案（如「未知阶段」）。

**说明**：旧版「加液→反应」等**细粒度用户步**已移除；细粒度动作仅存在于 **`businessPhase == 反应`（等）的内部 HAL 脚本**，不单独出现在状态栏。
